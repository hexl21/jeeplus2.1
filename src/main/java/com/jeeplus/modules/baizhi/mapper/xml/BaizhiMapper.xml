<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeeplus.modules.baizhi.mapper.BaizhiMapper">
    
	<sql id="baizhiColumns">
		a.id AS "id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		a.baizhi_name AS "baizhiname",
		a.baizhi_price AS "baizhiprice",
		a.baizhi_sge AS "baizhisge"
	</sql>
	
	<sql id="baizhiJoins">
		
	</sql>
	
    
	<select id="get" resultType="Baizhi" >
		SELECT 
			<include refid="baizhiColumns"/>
		FROM test_baizhi a
		<include refid="baizhiJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Baizhi" >
		SELECT 
			<include refid="baizhiColumns"/>
		FROM test_baizhi a
		<include refid="baizhiJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			${dataScope}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Baizhi" >
		SELECT 
			<include refid="baizhiColumns"/>
		FROM test_baizhi a
		<include refid="baizhiJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			${dataScope}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO test_baizhi(
			id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag,
			baizhi_name,
			baizhi_price,
			baizhi_sge
		) VALUES (
			#{id},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag},
			#{baizhiname},
			#{baizhiprice},
			#{baizhisge}
		)
	</insert>
	
	<update id="update">
		UPDATE test_baizhi SET 	
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			baizhi_name = #{baizhiname},
			baizhi_price = #{baizhiprice},
			baizhi_sge = #{baizhisge}
		WHERE id = #{id}
	</update>
	
	
	<!--物理删除-->
	<update id="delete">
		DELETE FROM test_baizhi
		WHERE id = #{id}
	</update>
	
	<!--逻辑删除-->
	<update id="deleteByLogic">
		UPDATE test_baizhi SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	
	<!-- 根据实体名称和字段名称和字段值获取唯一记录 -->
	<select id="findUniqueByProperty" resultType="Baizhi" statementType="STATEMENT">
		select * FROM test_baizhi  where ${propertyName} = '${value}'
	</select>
	
</mapper>